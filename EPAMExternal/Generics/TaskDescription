Generics
Общие требования
•	Реализация должна базироваться на паттерне проектирования «Command».
•	Не использовать интерфейсы для патерна «Command» из стандартной библиотеки.

Формулировка задания
Спроектировать и реализовать Command Executor. Реализация должна состоять из нескольких компонентов. 
Должны существовать компоненты «Команда» и «Исполнитель (Executor)». 
Команда должна иметь возможность исполняться. Исполнением команд занимается исполнитель. 
Для команды и исполнителя должны существовать интерфейсы и реализации. Для исполнителя достаточно одной реализации, для команд необходимо написать несколько реализаций.
Команда должна уметь возвращать результат своего выполнения. Этот результат может быть любого типа (1). Уточнение типа допускается (2).
Исполнитель принимает команду, запускает ее и возвращает результат ее выполнения (3).
Написать клиентский (4) код. В нем продемонстрировать работу исполнителя: создать несколько различных команд, передать их исполнителю (5) получить результат выполнения и обработать его (6).

Сноски
1.	Использовать дженерики.
2.	Использование вайлдкард, super, extends.
3.	Исполнитель не должен знать о типе результата выполнения команды (использовать типизированный метод).
4.	Код, который является клиентом исполнителя, использующий исполнитель для своих целей.
5.	Допускается передавать команды последовательно, по одной.
6.	Например, вывести в консоль. Допускаются и другие варианты, демонстрирующие или использующие результат выполнения команды.
