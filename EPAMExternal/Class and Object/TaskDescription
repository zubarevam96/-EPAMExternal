Задание Б
1. Выбрать предментную область и не ее основе создать интефейс (условно M) и несколько
классов, этот интерфейс реализующих.
2. Сгенерировать массив M[], случайно заполненний объектами созданных классов
3. Создать сервис, возвращающий M[][], где каждый подмассив заполнен объектами одного
класса. Пример: [A, A, B, A, B, B A, A] -&gt; [A, A, A, A, A], [B, B, B].
4. Продемонстрировать через вывод на консоль, что каждый отдельный массив содержит
объекты только одного класса.
